# --- Web 伺服器與 API 架構 ---
fastapi
uvicorn[standard]
python-multipart
pydantic

# --- 影像處理與資料科學 ---
# 註：如果在沒有 GUI (桌面環境) 的純文字 Linux 伺服器上，
# 建議把 opencv-python 改成 opencv-python-headless 以避免缺少 .so 函式庫錯誤
opencv-python
numpy
pandas

# --- AI 與深度學習模組 ---
easyocr
ultralytics
ollama